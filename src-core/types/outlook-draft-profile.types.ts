/** Profil Outlook proposé pour inscription manuelle (hors nodriver). */
export type OutlookDraftProfile = {
  id: string
  selected: boolean
  firstName: string
  lastName: string
  birthday: string
  email: string
  password: string
  saved: boolean
}
