/**
 * Identifiant du sidecar Broward tel que declare dans `tauri.conf.json` > `bundle.externalBin`
 * et dans `capabilities/default.json` (permissions shell: spawn, execute, kill).
 */
export const BROWARD_ENROLLMENT_SIDECAR_NAME: string = 'binaries/broward-enrollment'
