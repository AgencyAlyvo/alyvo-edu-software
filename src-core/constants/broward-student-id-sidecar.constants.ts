/**
 * Identifiant du sidecar Student ID tel que declare dans `tauri.conf.json` > `bundle.externalBin`.
 */
export const BROWARD_STUDENT_ID_SIDECAR_NAME: string = 'binaries/broward-student-id'

/** Delai minimum (heures) apres envoi demande ecole avant eligibilite Student ID. */
export const BROWARD_STUDENT_ID_MIN_HOURS_AFTER_REQUEST: number = 3
